# 05. 4단계 — 동시 로그인 방지 (자율)

> 이 단계의 핵심 질문: **stateless한 인증 구조가 운영 요구사항과 충돌하면 어떻게 풀 것인가?**
>
>
> **완성이 아니라 "막힌 지점 이해"가 목표.**
>
> 이 문서는 **6번 대화 (Sonnet)** 에서 채워진다.
>

---

## 1. 요구사항 요약 (가이드 발췌)

### 목표

- 동시 로그인 방지 요구사항이 인증 방식과 어떻게 연결되는지 이해한다.
- 세션 방식에서 동시 로그인을 제한하는 방법을 검토한다.
- 토큰 방식에서 이미 발급된 토큰을 무효화하는 어려움을 경험한다.
- stateless한 구조가 운영 요구사항과 충돌할 수 있음을 이해한다.

### 요구사항

- 같은 계정으로 여러 기기에서 동시에 로그인하는 상황을 제한한다.
- 새 로그인이 발생했을 때 기존 로그인 상태를 어떻게 처리할지 정한다.
- 기존 로그인을 유지할지, 새 로그인을 우선할지 정책을 정한다.
- 세션 또는 토큰 방식에서 이 요구사항을 구현할 때 어떤 어려움이 있는지 정리한다.

### 구현 조건

- 어떤 지점에서 어려웠는지 설명하면 된다. (코드 완성 강제 아님)
- 현재 미션에서 선택한 인증 방식을 기준으로 먼저 검토한다.

### 완료 기준

- [ ]  동시 로그인 방지 요구사항을 현재 인증 방식에서 검토했다.
- [ ]  구현하거나 설계한 방식을 설명했다.
- [ ]  막힌 지점과 알게 된 점을 정리했다.
- [ ]  토큰의 stateless한 특성이 깨질 수 있는 지점을 인식했다.

---

## 2. 내가 선택한 인증 방식에서의 출발 상태

- 1·2단계에서 선택한 인증 방식:
- 현재 서버가 "누가 로그인 중인지" 알고 있는가:
- 미리 추측하는 막힐 지점:
    - 세션이라면: 같은 계정의 세션이 여러 개 생길 때 어떻게 식별/정리?
    - 토큰이라면: 이미 발급된 토큰을 어떻게 무효화? 매번 DB 조회하면 stateless가 아닌 것 아닌가?

---

## 2.5 생각해 볼 점 (가이드 원문)

가이드가 단계 시작 전 던지는 사고 자극 질문들. 답은 슬롯·4번에서 점차 나옴.
**이 단계는 "막힌 지점 이해"가 목표라 답이 "잘 모르겠다" 또는 "여기서 막혔다"로 끝나도 됨.** 막힌 자리 자체가 학습.
체크박스는 단계 끝에서 "이 질문에 대한 내 답이 어딘가 들어갔는가" 점검용.

- [ ]  **서버는 현재 로그인한 사용자의 상태를 알고 있는가?** → 2 + 3.2 (인증 방식이 만든 출발점)
- [ ]  **새 로그인이 발생했을 때 기존 로그인 정보를 찾을 수 있는가?** → 3.1 + 3.2 (충돌 정책, 저장 위치)
- [ ]  **이미 발급된 토큰을 무효화할 수 있는가?** → 3.4 (stateless 포기 여부)
- [ ]  **토큰을 검증할 때마다 서버 저장소를 확인하면 여전히 stateless하다고 볼 수 있는가?** → 3.3 + 3.4, 그리고 8번 (가장 핵심적인 사고 질문)
- [ ]  **운영 요구사항을 만족하기 위해 어떤 상태를 추가로 저장해야 하는가?** → 3.2 + 6 (막힌 지점)

---

## 3. 가이드가 명시한 선택 카테고리

### 3.1 충돌 정책

**가이드 질문:** 기존 로그인을 유지할 것인가, 새 로그인을 우선할 것인가?

| 후보 | 동작 | 장점 | 단점 | 내 선택 |
| --- | --- | --- | --- | --- |
| A. 기존 로그인 유지, 새 로그인 거부 | 두 번째 기기에 401 | 사용자 의도 명확, 보안 | UX 불편 (재로그인 안 됨) |  |
| B. 새 로그인 우선, 기존 무효화 | 두 번째 기기 성공 + 첫 기기 다음 요청부터 401 | 일반적 UX | 첫 기기는 갑자기 끊어짐 |  |
| C. 둘 다 허용 (현재 상태) | 변경 없음 | 단순 | 요구사항 불충족 |  |

**선택:**

**선택 이유:**

---

### 3.2 상태 저장 위치

| 후보 | 동작 | 장점 | 단점 | 내 선택 |
| --- | --- | --- | --- | --- |
| A. 인메모리 (`Map<MemberId, SessionId/TokenId>`) | 서버 재시작 시 소실 | 단순 | 확장 불가, 휘발 |  |
| B. DB 테이블 (`active_session`) | 영속 | 다중 서버 가능 | 매 요청 조회 비용 |  |
| C. 별도 캐시 (Redis 등) | 빠른 조회 | 운영 친화 | 인프라 추가 |  |

**선택:**

**선택 이유:**

---

### 3.3 검증 시점

| 후보 | 동작 | 장점 | 단점 | 내 선택 |
| --- | --- | --- | --- | --- |
| A. 매 요청마다 저장소 조회 | 즉시 무효화 가능 | 정확 | stateless 깨짐 / 성능 부담 |  |
| B. 로그인 시점에만 확인 + 토큰 짧은 만료 | 매 요청 조회 없음 | stateless 유지 | 무효화에 최대 만료시간 지연 |  |

**선택:**

**선택 이유:**

---

### 3.4 토큰 방식에서의 stateless 포기 여부

*(토큰을 선택한 경우에만)*

| 후보 | 동작 | 트레이드오프 |
| --- | --- | --- |
| A. 부분적 stateful (블랙리스트 / `active_token_id`) | "토큰은 stateless"가 깨지는 지점 명확히 인식 |  |
| B. 완전 stateless 유지 + 짧은 만료로 우회 | 동시 로그인 방지는 best-effort에 그침 |  |
| C. Refresh Token 모델로 전환 | 구조 변경 부담 큼 |  |

**선택:**

**선택 이유:**

---

## 4. 구현 중 새로 마주친 결정들

### 4.1 ___

*(예상되는 후보들)*

- 동시 로그인 방지를 모든 사용자에 적용할지, 매니저에게만 적용할지
- 충돌 정책을 사용자가 선택할 수 있게 할지 (옵션화) — 보통 아님
- 강제 로그아웃된 클라이언트에게 별도 응답 코드를 줄지 (401과 419 등 분리)
- 테스트로 검증할지, 설계만 정리할지

---

## 5. 시도한 방식

(코드를 작성했다면) 어떤 구조로 만들었는지 간단히.

---

## 6. 막힌 지점

가장 중요한 섹션. 가이드가 강조한 "어디에서 어려움이 생기는지 이해"의 본체.

- ⚠️ ___
- ⚠️ ___

---

## 7. 알게 된 점

- ...
- ...

---

## 8. 토큰의 stateless 특성이 깨지는 지점 (해당 시)

- ...

---

## 9. 다시 한다면 바꾸고 싶은 점

- 인증 방식 자체를 ___로 했으면 동시 로그인 방지가 더 쉬웠을 것
-

---

## 10. 평가 기준 충족 한 줄씩

1. **공통 처리 분리:**
2. **다른 후보와 비교 설명:**
3. **트레이드오프 인식:**
4. **운영 위협 시나리오 경험:** ← 이번 단계의 핵심 평가 기준

---

## 10.5 "생각해 볼 점"에 대한 4단계 최종 답변

2.5에서 던진 질문들에 대해, 4단계를 모두 마친 시점에서 정리한 답.

- **체크박스:** 2.5의 체크박스를 여기에 다 ✅로 만들 수 있어야 단계 종료.
- **답이 "여기서 막혔다 / 모르겠다"여도 됨.** 4단계는 "어디서 막히는지 이해"가 목표.
- **분량:** 각 질문당 2~4줄.

### Q1. 서버는 현재 로그인한 사용자의 상태를 알고 있는가?

> *(2번과 3.2의 결과 — 인증 방식이 만든 출발점에서, 4단계 요구사항을 만난 첫 답)*
>

**답:**

### Q2. 새 로그인이 발생했을 때 기존 로그인 정보를 찾을 수 있는가?

> *(3.1 + 3.2의 결과 — 충돌 정책과 저장 위치)*
>

**답:**

### Q3. 이미 발급된 토큰을 무효화할 수 있는가?

> *(3.4의 결과 — 토큰 방식에서 가장 부딪치는 지점)*
>

**답:**

### Q4. 토큰을 검증할 때마다 서버 저장소를 확인하면 여전히 stateless하다고 볼 수 있는가?

> *(8번의 결과 — 이 단계의 핵심 통찰. "stateless"라는 말의 한계가 어디서 드러났는지)*
>

**답:**

### Q5. 운영 요구사항을 만족하기 위해 어떤 상태를 추가로 저장해야 하는가?

> *(3.2 + 6번의 결과 — 막힌 지점에서 도출된 답)*
>

**답:**

---

## 11. PR 본문 조각

```
4단계 - 동시 로그인 방지 (자율)
- 시도한 방식:
- 막힌 지점:
- 알게 된 점:
- 다시 한다면 바꾸고 싶은 점:
```
